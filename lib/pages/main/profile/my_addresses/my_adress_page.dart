import 'package:flutter/material.dart';

class MyAdressPage extends StatelessWidget {
  const MyAdressPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("My Adress Page"),
      ),
    );
  }
}

